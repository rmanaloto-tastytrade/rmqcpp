# rmqcpp-order-status-monitor AI Findings

- **High – Event loop control assumptions (lines 57-58):** Doc suggests supplying our own `io_context` and preferring io_uring/epoll. rmqcpp owns a single [`Boost.Asio`](https://www.boost.org/doc/libs/latest/doc/html/boost_asio.html) event loop inside `RabbitContext`; callers do not inject an `io_context`, and io_uring is not documented [~/dev/github/bloomberg/rmqcpp/[docs/eventloop.md](eventloop.md):1-8]. *Amend:* describe running the `RabbitContext`’s loop on a dedicated thread if desired, without assuming io_uring support or pluggable contexts.
- **High – Heartbeat/reconnect configurability overstated (lines 63-69, 91-96):** Heartbeats are mandatory and negotiated; library plans only future ability to lower interval and does not expose disable/override today [~/dev/github/bloomberg/rmqcpp/[docs/heartbeats.md](heartbeats.md):1-17]. Reconnect is automatic with no public backoff knobs [~/dev/github/bloomberg/rmqcpp/README.md:21-36]. *Amend:* clarify that heartbeat interval/backoff are not configurable now; capture as a gap if needed rather than implying a setting.
- **Medium – TLS surface mismapped (lines 37-46, 63-65, 78-85):** Text references legacy `Channel::CreateSecure` and hostname verification knobs. rmqcpp expects `rmqt::SecureEndpoint` + `rmqt::SecurityParameters`, which requires a CA path and supports verification modes `VERIFY_SERVER` or `MUTUAL` plus optional client cert/key [~/dev/github/bloomberg/rmqcpp/src/rmq/rmqt/rmqt_secureendpoint.h:30-48, ~/dev/github/bloomberg/rmqcpp/src/rmq/rmqt/rmqt_securityparameters.h:25-45]. *Amend:* rewrite TLS section/config to match these parameters and note there is no “disable verification” mode.
- **Medium – Producer/consumer defaults under-specified (lines 62-68, 99-109):** rmqcpp enforces publisher confirms, manual consumer acks, and sets the mandatory flag true by default [~/dev/github/bloomberg/rmqcpp/README.md:26-39, 134-167]. Current doc does not call these out, leaving ambiguity on ack/confirm expectations. *Amend:* state that handlers must ack/nack, confirms must be observed (e.g., `waitForConfirms` on shutdown), and mandatory=true aligns with legacy dead-lettering needs.
- **Low – Recovery semantics missing (lines 69-73):** rmqcpp will redeclare cancelled consumers on reconnect, which is important for stability [~/dev/github/bloomberg/rmqcpp/README.md:40-41]. *Amend:* note this behavior and ensure adapter wiring does not layer its own resubscribe logic that could duplicate declarations.
- **Low – Test/example references incomplete (lines 135-142):** Integration tests and performance harnesses are omitted; quick start in upstream README points to integration suites, and there is a pytest-based set in `src/tests/integration` [~/dev/github/bloomberg/rmqcpp/README.md:71, ~/dev/github/bloomberg/rmqcpp/src/tests/integration/README.md:1-3]. *Amend:* cite these as parity checks for the adapter and as runnable examples beyond the listed samples.
